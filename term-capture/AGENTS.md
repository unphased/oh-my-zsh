After each unit of requested changes, create a commit by running `git commit -am "<commit message here>"`.

When working on tests/coverage, review the latest coverage stats in `debug/coverage/coverage.txt` and prefer adding deterministic unit tests to close gaps.
