# custom files
custom/

# temp files directories
cache/
log/
*.swp
.DS_Store

# editor configs
.vscode
.idea
.aider*

# build directories and artifacts
term-capture/debug/
term-capture/release/
term-capture/*.o
term-capture/*.input
term-capture/*.output
term-capture/*.tidx
term-capture/*.events.jsonl
term-capture/*.meta.json
term-capture/*.ws.json

termplex/debug/
termplex/release/
termplex/*.o
termplex/*.input
termplex/*.output
termplex/*.tidx
termplex/*.events.jsonl
termplex/*.meta.json
termplex/*.ws.json

# zcompile cached files
*.zwc

# place i keep scratch capture files for now
captures/
